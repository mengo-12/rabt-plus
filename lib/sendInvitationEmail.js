export default async function sendInvitationEmail(email, teamId) {
    // هنا من الممكن استخدام SendGrid أو Resend أو أي خدمة بريد
    console.log(`Invitation sent to ${email} for team ${teamId}`);
    // ... كود إرسال البريد الفعلي
}